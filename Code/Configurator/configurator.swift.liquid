//
//  {{ prefix }}{{ module_info.name }}{{ module_info.file_name }}
//  {{ module_info.project_name }}
//
//  Created by {{ developer.name }} on {{ date }}.
//  Copyright © {{ year }} {{ developer.company }}. All rights reserved.
//

import UIKit

class {{ module_info.name }}ModuleConfigurator {

    static func configureModule() -> UIViewController {

        guard let viewController = UIStoryboard(name: "{{ module_info.name }}ViewController", bundle: nil).instantiateInitialViewController() as? PhoneViewController else {
            fatalError("Can't instantiate ViewController")
        }

        let router = {{ module_info.name }}Router(viewController: viewController)

        let presenter = {{ module_info.name }}Presenter()
        presenter.view = viewController
        presenter.router = router

        let interactor = {{ module_info.name }}Interactor()
        interactor.output = presenter

        presenter.interactor = interactor
        viewController.output = presenter

        return viewController
    }
}
